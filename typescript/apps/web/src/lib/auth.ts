/**
 * Server-side auth configuration
 *
 * NOTE: This file is a placeholder. The actual server-side auth configuration
 * should be in a separate server/API package, not in the web client.
 *
 * For client-side auth, use auth-client.ts instead.
 */

// Export a placeholder to prevent import errors
export const auth = null

// The actual auth configuration should be in a server package like:
// - apps/api/src/lib/auth.ts
// - packages/auth/src/index.ts
